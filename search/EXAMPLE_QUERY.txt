Example conceptual query:

("LLM agent*" OR "large language model agent*" OR "language model agent*")
AND
("persistent memory" OR "long-term memory" OR "agent memory")
AND
(forensic* OR provenance OR lineage OR traceback OR attribution
 OR "incident reconstruction" OR "causal reconstruction"
 OR rollback OR recovery OR "blast radius")
